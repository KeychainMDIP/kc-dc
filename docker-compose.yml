version: "3"
services:

  gatekeeper:
    image: macterra/gatekeeper
    volumes:
      - ./data:/app/data

  hyperswarm:
    image: macterra/hyperswarm-mediator
    environment:
      - GATEKEEPER_URL=http://gatekeeper:3000
    volumes:
      - ./data:/app/data
